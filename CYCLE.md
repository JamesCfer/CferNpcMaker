# Feature Cycle Instructions

Paste the block below to Claude Code at the start of each session to implement the next feature.

---

```
Read TODO.md and find the first unchecked item. Implement it fully across all relevant files in the shared/ and modules/ directories. Follow the existing code conventions — no comments unless the why is non-obvious, no abstractions beyond what the feature needs. When done, check the item off in TODO.md, commit everything to the branch claude/confident-planck-Ugc8G, push, and merge into main. Report what you built in two sentences.
```
